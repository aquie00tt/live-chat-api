/**
 * Type representing the possible statuses for responses.
 *
 * - "fail": Indicates a failed operation or request.
 * - "error": Indicates an error that occurred during processing.
 */
export type Status = "fail" | "error";
